services:
  # Backend API Service
  - type: web
    name: AI(Awesome Intelligence)
    runtime: node
    region: oregon
    plan: free
    branch: main
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: GITHUB_TOKEN
        sync: false
      - key: PORT
        value: 3000
    healthCheckPath: /api/health
